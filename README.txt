The main stuff I made is in the http-server > public

Since this is a angular component the majority of this app is inside the components/calculator folder.

PLEASE IGNORE calculator.js that is not inside the calculator folder(problem with git) 

